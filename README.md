# GraphLoom

A visual dataflow programming environment — weaving shell commands, Python, SQL, git and any REPL into an interactive node graph on an infinite canvas.

## Vision

Every prompt is a node. Every pipe is an edge. The graph is the program.

Navigate between nodes by keyboard. Zoom out to see the whole system. Drill into any node to see the graph within.

## Status

Early development.
